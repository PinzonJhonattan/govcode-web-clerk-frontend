import { UserHasPermissionsDirective } from './user-has-permissions.directive';

describe('UserHasPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserHasPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
